import React from 'react'
import Link from 'gatsby-link'

export default () => (
  <div>
    <h1>Looks like that page stopped existing.</h1>
    <p>So sorry, but this link appears to be broken.</p>
    <p>(<Link to="/">back to the home page</Link>)</p>
  </div>
)
